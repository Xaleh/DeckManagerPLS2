<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.alexandresilvareis.deckmangerpls2">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_personal"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_personal_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".OnTheGridActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FormularioActivity" >
            <!--<intent-filter>
                &lt;!&ndash;<action android:name="android.intent.action.MAIN" />&ndash;&gt;

                &lt;!&ndash;<category android:name="android.intent.category.LAUNCHER" />&ndash;&gt;
            </intent-filter>-->
        </activity>
    </application>

</manifest>